=============
 Datastorage
=============

The datastorage utility provides transparent access to the database
(which is currently ZODB).

>>> import zope.component
>>> import colony.interfaces
>>> storage = zope.component.getUtility(colony.interfaces.IDatabase)
>>> storage
<colony.database.TestDatabase object at 0x...>

