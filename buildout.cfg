[buildout]
develop = .
parts = test
        colony

[config]
database = ${buildout:directory}/var/Data.fs
configdir = ${buildout:directory}/configs

[colony]
recipe = iw.recipe.template:script
source = ${buildout:directory}/instance.tmpl
destination = ${buildout:bin-directory}/

[test]
recipe = zc.recipe.testrunner
eggs = colony
